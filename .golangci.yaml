linters:
  enable-all: true
  disable:
    - exportloopref
    - depguard
    - wrapcheck

issues:
  exclude-rules:
    - path: 'cmd/'
      linters:
        - gochecknoinits
        - exhaustruct
        - gochecknoglobals
        - forbidigo
        - revive
