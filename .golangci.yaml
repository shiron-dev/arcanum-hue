linters:
  enable-all: true
  disable:
    - exportloopref
    - depguard
    - wrapcheck
    - testpackage

issues:
  exclude-rules:
    - path: 'cmd/'
      linters:
        - gochecknoinits
        - exhaustruct
        - gochecknoglobals
        - forbidigo
        - revive
